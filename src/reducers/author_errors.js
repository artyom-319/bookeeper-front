const errorsInitialState = {
    occurred: false,
    message: '',
};

const initialState = {
    plain: errorsInitialState,
    form: errorsInitialState,
    errorOccurred: false,
    errorMessage: '',
};
